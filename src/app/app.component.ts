import { Component } from '@angular/core';

@Component({
    selector: 'app',
    template: `
    <div class="container">
        <app-blogs></app-blogs>
    </div>
    
    `
})
export class AppComponent { }